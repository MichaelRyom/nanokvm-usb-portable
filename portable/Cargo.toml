[package]
name = "nanokvm-usb-portable"
version = "1.4.0"
edition = "2021"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
rust-embed = "8"
mime_guess = "2"

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
